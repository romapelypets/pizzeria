@model IEnumerable<Pizzeria.DAL.Models.Product>

@{
    ViewBag.Title = "Toppings";
}

@section scripts{
    <script type="text/javascript">
        $(document).ready(function () {
            $('.Add').click(function () {
                location.reload();
            });
        });

       
    </script>
}

@*<h2>Toppings</h2>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Time)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Time)
        </td>
        <td>
            @Html.ActionLink("Add to Pizza", "AddCustomToppping", "ShoppingCart", new { productId = @item.Id }, new { @class = "Add" })
        </td>
    </tr>
}

    @Html.ActionLink("Add to Cart", "AddToCart", "ShoppingCart", new { id = 2 }, new { @class = "btn btn-ifo" })

</table>

@TempData["Price"]*@




<div class="container">
    <div class="row">
        <div class="col-sm-12 col-md-10 col-md-offset-1">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th></th>
                        <th>Price</th>
                    
                        <th></th>
                    </tr>
                </thead>
                @foreach (var item in Model)
                {
                    <tbody>
                        <tr>
                            <td class="col-sm-8 col-md-6">@item.Name</td>
                            <td></td>
                            <td class="col-sm-1 col-md-1 text-center"><strong>@item.Price</strong></td>
                            <td class="col-sm-1 col-md-1 text-center">@Html.ActionLink("Add to Pizza", "AddCustomToppping", "ShoppingCart", new { productId = @item.Id }, new { @class = "Add btn btn-warning" })</td>
                        </tr>
                    </tbody>
                }
                <tfoot>
                    <tr>
                        <td></td>
                        <td></td>
                        
                        <td><h3>Total</h3></td>
                        <td class="text-success text-right"><h3>@TempData["Price"]</h3></td>
                    </tr>
                    <tr>
                        <td></td>
                        <td></td>
                        <td>
                            
                        </td>

                        <td>@Html.ActionLink("Add to Cart", "AddToCart", "ShoppingCart", new { id = 2 }, new { @class = "btn btn-success" })</td>
                    </tr>
                </tfoot>
            </table>
        </div>
    </div>
</div>





@*@if (TempData["Products"] != null)
{
foreach (var p in TempData["Products"] as List<string>)
{
    @p;
}}*@

